{
  "name": "food-server",
  "version": "1.1.0",
  "description": "A Node.js web server that fetches data from a third-party (Swiggy) API and exposes it to a client (Food) app.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon index.js"
  },
  "author": "Saddam Hussein",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cross-fetch": "^4.0.0",
    "dotenv": "^16.5.0",
    "express": "^4.18.2"
  }
}
